/*
  Desert Byte Main Styles
  Author: Zeyad (last updated Aug 2026)
  TODO: Refactor all the hardcoded colors into variables later.
  */

  :root {
    --brand-primary: #f59e0b; /* Clinet changed this from red to amber last week */
    --bg-dark: #0b0f19;
    --text-main: #334155;
    --gray-bg: #f8fafc;
  }

  /* Reset - kinda basic but works*/
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  body {
    font-family: 'Public Sans', sans-serif;
    color: var(--text-main);
    background-color: #fff;
    line-height: 1.6;
    /* overflow-x: hidden; */
  }

  a {
    text-decoration: none;
    color: inherit;
  }

  .inner-wrap-90 {
    width: 90%;
    max-width: 1150px;
    margin: 0 auto;
  }

  /* --- Header & Nav --- */
  #nav_main_header {
    position: fixed;
    top: 0;
    width: 100%;
    padding: 24px 0;
    background: transparent;
    z-index: 1000;
    transition: all 0.25s ease-in-out; /* smooth shrink */
  }

  /* JS adds this class when scrolling */
  #nav_main_header.scrolled-down {
    background: #ffffff;
    padding: 12px 0;
    box-shadow: 0 4px 20px rgba(0,0,0,0.06);
  }

  #nav_main_header .inner-wrap-90 {
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .brandLogo_txt {
    font-size: 1.5rem;
    font-weight: 700;
    color: #fff;
    transition: 0.2s;
  }

  .brandLogo_txt span {
    font-size: 0.8rem;
    font-weight: 400;
    color: var(--brand-primary);
  }

  #nav_main_header.scrolled-down .brandLogo_txt {
    color: var(--bg-dark);
  }

  #desktopMenu ul {
    list-style: none;
    display: flex;
    gap: 35px;
    align-items: center;
  }

  #desktopMenu a {
    color: #fff;
    font-weight: 600;
    font-size: 0.95rem;
  }

  #nav_main_header.scrolled-down #desktopMenu a {
    color: var(--text-main);
  }

  .cta-nav-btn {
    background: var(--brand-primary);
    padding: 10px 20px;
    border-radius: 4px;
    color: #111 !important;
  }

  #mob-trigger {
    display: none;
    width: 32px;
    height: 24px;
    cursor: pointer;
    position: relative;
  }
  #mob-trigger span {
    display: block;
    position: absolute;
    height: 3px;
    width: 100%;
    background: #fff;
    border-radius: 3px;
  }
#mob-trigger span:nth-child(1) { top: 0;}
#mob-trigger span:nth-child(2) { top: 10px; }
#mob-trigger span:nth-child(3) { top: 20px; }

#nav_main_header.scrolled-down #mob-trigger span {
    background: #111;
}

/* --- Hero Section --- */
.layout-hero-grid {
    display: flex;
    min-height: 85vh;
    align-items: center;
    padding-top: 80px;
}

.left-copy-area {
    max-width: 650px;
}

.left-copy-area h1 {
    font-size: 3.5rem;
    color: #fff;
    line-height: 1.1;
    margin-bottom: 25px;
    letter-spacing: -1px;
}

.sub-lead {
    font-size: 1.15rem;
    color: #cbd5e1;
    margin-bottom: 40px;
}

#btn-hero-primary {
    display: inline-block;
    background-color: var(--brand-primary);
    color: #000;
    padding: 14px 28px;
    font-weight: bold;
    border-radius: 6px;
    font-size: 1.1rem;
}

/* --- Services / Work --- */
.bg-alt-gray {
    background-color: var(--grey-bg);
    padding: 100px 0;
}

.sec-heading {
    font-size: 2.2rem;
    margin-bottom: 50px;
    color: var(--bg-dark);
}

.grid-services-3col {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 30px;
}

.srv-card {
    background: #fff;
    padding: 30px;
    border-radius: 8px;
    box-shadow: 0px 2px 10px rgba(0,0,0,0.03);
    border-top: 4px solid var(--brand-primary);
}

.srv-card h3 {
    margin-bottom: 12px;
    font-size: 1.3rem;
}

/* --- Accordion FAQ --- */
#faq-section {
    padding: 80px 0;
}

.max-w-600 {
    max-width: 700px;
}

.acc-row {
    border: 1px solid #e2e8f0;
    margin-bottom: 15px;
    border-radius: 5px;
}

.acc-trigger {
    padding: 18px;
    margin: 0;
    cursor: pointer;
    display: flex;
    justify-content: space-between;
    background: #fdfdfd;
    user-select: none;
}

.hidden-panel {
    display: none;
    padding: 0 18px 18px 18px;
    color: #475569;
}
.hidden-panel.show {
    display: block;
}

/* --- Contact & Footer --- */
.footer-contact {
    background-color: var(--bg-dark);
    color: #fff;
    padding: 100px 0;
}

.flex-split {
    display: flex;
    gap: 60px;
    justify-content: space-between;
}

.loc-info {
    width: 40%;
}
.loc-info h2 { margin-bottom: 20px; font-size: 2rem;}
.office-addr {
    margin: 30px 0;
    font-style: normal;
    color: #94a3b8;
    line-height: 1.8;
}
.direct-mail a {color: var(--brand-primary); text-decoration: underline;}

.form-container-box {
    width: 50%;
    background: #1e293b;
    padding: 40px;
    border-radius: 8px;
}

.form_row {
    margin-bottom: 20px;
}
.form_row label {
    display: block;
    margin-bottom: 8px;
    font-size: 0.9rem;
}
.form_row input, .form_row textarea {
    width: 100%;
    padding: 12px;
    border: 1px solid #334155;
    background: #0f172a;
    color: #fff;
    border-radius: 4px;
    font-family: inherit;
}

.err-txt {
    color:#ef4444; /* red-500 */
    font-size: 0.8rem;
    display: block;
    margin-top: 5px;
}

#btn_submit_lead {
  background: var(--brand-primary);
  border: none;
  padding: 15px;
  width: 100%;
  font-weight: bold;
  cursor: pointer;
  border-radius: 4px;
  font-size: 1rem;
}

#status_msg { margin-top: 15px; text-align: center; }

.site-ftr {
  background: #000;
  padding: 20px 0;
  color: #666;
  text-align: center;
  font-size: 0.9rem;
}

@media screen and (max-width: 850px) {
  .flex-split { flex-direction: column; }
  .loc-info, .form-container-box { width: 100%; }
  .grid-services-3col { grid-template-columns: 1fr; }
  .left-copy-area h1 { font-size: 2.5rm; }

  #mob-trigger { display: block; }
  #desktopMenu {
    display: none;
    position: absolute;
    top: 100%;
    left: 0;
    width: 100%;
    background: #fff;
    padding: 20px 0;
    border-bottom: 1px solid #eee;
  }
  #desktopMenu.open-mobile {
    display: block;
  }
  #desktopMenu ul {
    flex-direction: column;
    gap: 20px;
  }
  #desktopMenu a {
    color: var(--text-main) !important; /* override the white text */
  }
}
/* Pricing Grid */
.pricing-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 40px; max-width: 800px; margin: 0 auto; }
.price-card { background: #fff; border: 1px solid #e2e8f0; padding: 40px; border-radius: 8px; position: relative; }
.popular-plan { border: 2px solid var(--brand-primary); box-shadow: 0 10px 30px rgba(245, 158, 11, 0.15); transform: translateY(-10px); }
.badge { position: absolute; top: -12px; right: 20px; background: var(--brand-primary); color: #000; font-weight: bold; padding: 4px 12px; border-radius: 20px; font-size: 0.8rem; }
.price-amount { font-size: 2.5rem; font-weight: bold; color: var(--bg-dark); margin: 20px 0; }
.price-amount span { font-size: 1rem; color: #64748b; font-weight: normal; }
.price-features { list-style: none; margin-bottom: 30px; }
.price-features li { margin-bottom: 15px; color: #475569; }

/* Slider */
.slider-wrapper { overflow: hidden; max-width: 600px; margin: 0 auto; text-align: center; }
.slider-track { display: flex; transition: transform 0.4s ease-in-out; }
.slide-item { min-width: 100%; padding: 20px; }
.slide-item p { font-size: 1.2rem; font-style: italic; margin-bottom: 15px; }
.slide-item h4 { color: var(--brand-primary); }
.slider-controls { margin-top: 20px; }
.slider-controls button { background: none; border: 1px solid #cbd5e1; padding: 8px 16px; margin: 0 5px; cursor: pointer; border-radius: 4px; }
.slider-controls button:hover { background: #e2e8f0; }

/* Scroll Progress & Back to Top */
#scroll-progress { position: fixed; top: 0; left: 0; height: 4px; background: var(--brand-primary); width: 0%; z-index: 9999; }
#back-to-top { position: fixed; bottom: 30px; right: 30px; background: var(--bg-dark); color: #fff; width: 40px; height: 40px; border: none; border-radius: 50%; font-size: 1.2rem; cursor: pointer; opacity: 0; transition: 0.3s; pointer-events: none; z-index: 100; }
#back-to-top.visible { opacity: 1; pointer-events: auto; }

/* Terminal Easter Egg */
.hidden-modal { display: none; position: fixed; inset: 0; background: rgba(0,0,0,0.8); z-index: 99999; align-items: center; justify-content: center; }
.hidden-modal.show-term { display: flex; }
.terminal-window { background: #0f172a; width: 90%; max-width: 600px; height: 400px; border-radius: 8px; overflow: hidden; font-family: monospace; box-shadow: 0 20px 50px rgba(0,0,0,0.5); }
.term-header { background: #1e293b; padding: 10px; display: flex; align-items: center; gap: 8px; }
.term-btn { width: 12px; height: 12px; border-radius: 50%; display: inline-block; }
.close-term { background: #ef4444; cursor: pointer; }
.min-term { background: #eab308; }
.max-term { background: #22c55e; }
.term-title { color: #94a3b8; font-size: 0.85rem; margin-left: 10px; }
.term-body { padding: 20px; color: #e2e8f0; height: 350px; overflow-y: auto; }
.input-line { display: flex; gap: 10px; margin-top: 10px; }
#term-input { background: transparent; border: none; color: #fff; font-family: monospace; font-size: 1rem; flex: 1; outline: none; }
body{
  cursor: none;
}
a, button, .acc-trigger, .term-btn {
  cursor: none;
}

.custom-cursor{
  position: fixed;
  top: 0;
  left: 0;
  width: 20px;
  height: 20px;
  border: 2px solid vra(--brand-primary);
  border-radius: 50%;
  pointer-events: none;
  transform: translate(-50%, -50%);
  z-index: 999999;
  transition: width 0.2s, height 0.2s, background-color 0.2s;
}

.custom-cursor.hovering {
  width: 45px;
  height:45px;
  background-color: rgba(245, 158, 11, 0.2);
  border-color: transparent;
}

.srv-card, .price-card {
  transition: transform 0.1s ease, box-shadow 0.1s ease;
  transform-style: preserve-3d;
  will-change: transform;
}
.dashboard-mockup { display: flex; gap: 30px; justify-content: center; flex-wrap: wrap; }
.dash-panel { background: #0f172a; border: 1px solid #1e293b; padding: 30px; border-radius: 12px; width:100%; max-width: 450px; box-shadow: 0 10px 30px rgba(0,0,0,0.5); }
.dash-panel h4 { margin-bottom: 20px; color: var(--brand-primary); font-size: 1.2rem; }
sensor-status { display: flex; align-items: center; gap: 15px; margin-bottom: 15px; font-family: monospace; font-size: 1.05rem; }
.indicator { width: 14px; height: 14px; border-radius: 50%; display: inline-block; }
.indicator.safe { background: #4ade80; box-shadow: 0 0 10px #4ade80; }
.indicator.warning { background: #facc15; box-shadow: 0 0 10px #facc15; }
.progress-bar-wrap { background: #1e293b; height: 24px; border-radius: 12px; overflow: hidden; margin-top: 20px; }
.progress-fill { background: linear-gradient(90deg, var(--brand-primary), #fbbf24); height: 100%; display: flex; align-items: center; justify-content: flex-end; padding-right: 15px; font-size: 0.8rem; color: #000; font-weight: bold; }
.pulse-anim { animation: pulseLoop 2s infinite ease-in-out; }
@keyframes pulseLoop { 0% {opacity: 1; transform: scale(1);} 50% {opacity: 0.4; transform: scale(0.8); } 100% {opacity: 1; transform: scale(1); } }
.file-drop-zone { border: 2px dashed #475569; padding: 40px 20px; text-align: center; border-radius: 8px; cursor: pointer; transition: all 0.3s ease; background: rgba(15, 23, 42, 0.5); }
.file-drop-zone p { color: #94a3b8; pointer-events: none; }
.file-drop-zone.dragover { border-color: var